<div>
  <h1 class="text-4xl"><%= @person["name"] %></h1>
  <div class="mt-3 flex space-x-3">
    <div class="grow flex flex-col space-y-3">
    </div>
    <% if @person["profile_path"] %>
      <%= image_tag("https://image.tmdb.org/t/p/w185#{@person["profile_path"]}", class: "") %>
    <% end %>
  </div>
 </div>
